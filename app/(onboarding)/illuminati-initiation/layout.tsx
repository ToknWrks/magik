export const metadata = {
  title: 'Illuminati Initiation',
  description: 'Begin your Illuminati initiation.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
